// Svelte interactive command handler — delegates to Vue's implementation since
// both are Vite-based bundles with identical chunk structure.
export { handleCommand } from "../../vue_js/interactive_helpers/commandHandler.js";
